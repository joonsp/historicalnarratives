# Backend API Key (Server-side only - never exposed to frontend)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Optional: Backend URL (defaults to http://localhost:3001)
# VITE_BACKEND_URL=http://localhost:3001
